{
  "name": "@wakey/browser-extension",
  "version": "0.1.0",
  "description": "Browser activity tracking extension for Wakey",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.34",
    "esbuild": "^0.19.0"
  }
}
