{
  "name": "wakey",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered productivity platform with 77 features",
  "author": "Wakey Team",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rimraf node_modules",
    "desktop": "pnpm --filter @wakey/desktop dev",
    "desktop:build": "pnpm --filter @wakey/desktop build"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "electron": "28.1.0"
  }
}
